<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ana Sayfa - NewsHub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-newspaper me-2"></i>NewsHub
            </a>
            
            <div class="navbar-nav ms-auto">
                <% if (user && user.id) { %>
                    <!-- Kullanıcı giriş yapmış -->
                    <div class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user me-1"></i><%= user.name %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/dashboard">Dashboard</a></li>
                            <li><a class="dropdown-item" href="/posts/new">Yazı Ekle</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form action="/auth/logout" method="POST" class="d-inline">
                                    <button type="submit" class="dropdown-item text-danger">
                                        <i class="fas fa-sign-out-alt me-1"></i>Çıkış
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </div>
                <% } else { %>
                    <!-- Kullanıcı giriş yapmamış -->
                    <a class="nav-link" href="/auth/login">
                        <i class="fas fa-sign-in-alt me-1"></i>Giriş
                    </a>
                    <a class="nav-link" href="/auth/register">
                        <i class="fas fa-user-plus me-1"></i>Kayıt
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <main class="container py-5">
        <div class="text-center">
            <h1 class="display-4 fw-bold mb-4">NewsHub'a Hoş Geldiniz!</h1>
            <p class="lead mb-4">Haber ve blog platformu</p>
            
            <% if (user && user.id) { %>
                <!-- Giriş yapmış kullanıcı için -->
                <div class="d-flex justify-content-center gap-3">
                    <a href="/posts" class="btn btn-primary btn-lg">Yazıları Görüntüle</a>
                    <a href="/posts/new" class="btn btn-success btn-lg">Yazı Ekle</a>
                    <a href="/dashboard" class="btn btn-outline-primary btn-lg">Dashboard</a>
                </div>
            <% } else { %>
                <!-- Giriş yapmamış kullanıcı için -->
                <div class="d-flex justify-content-center gap-3">
                    <a href="/posts" class="btn btn-primary btn-lg">Yazıları Görüntüle</a>
                    <a href="/auth/register" class="btn btn-success btn-lg">Kayıt Ol</a>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container text-center">
            <p class="mb-0">&copy; 2024 NewsHub</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>