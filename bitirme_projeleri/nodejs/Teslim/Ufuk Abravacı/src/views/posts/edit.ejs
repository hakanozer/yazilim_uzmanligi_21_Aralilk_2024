<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yazıyı Düzenle - NewsHub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">NewsHub</a>
            <a href="/posts/<%= post._id %>" class="btn btn-outline-light">Geri</a>
        </div>
    </nav>

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <h1 class="mb-4">Yazıyı Düzenle</h1>
                
                <% if (error) { %>
                    <div class="alert alert-danger">
                        <%= error %>
                    </div>
                <% } %>

                <form action="/posts/<%= post._id %>/update" method="POST">
                    <div class="mb-3">
                        <label for="title" class="form-label">Başlık</label>
                        <input type="text" class="form-control" id="title" name="title" 
                               value="<%= post.title %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="category" class="form-label">Kategori</label>
                        <select class="form-select" id="category" name="category" required>
                            <option value="technology" <%= post.category === 'technology' ? 'selected' : '' %>>Teknoloji</option>
                            <option value="science" <%= post.category === 'science' ? 'selected' : '' %>>Bilim</option>
                            <option value="health" <%= post.category === 'health' ? 'selected' : '' %>>Sağlık</option>
                            <option value="education" <%= post.category === 'education' ? 'selected' : '' %>>Eğitim</option>
                            <option value="business" <%= post.category === 'business' ? 'selected' : '' %>>İş Dünyası</option>
                            <option value="entertainment" <%= post.category === 'entertainment' ? 'selected' : '' %>>Eğlence</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="content" class="form-label">İçerik</label>
                        <textarea class="form-control" id="content" name="content" rows="12" required><%= post.content %></textarea>
                    </div>

                    <div class="mb-4">
                        <label for="tags" class="form-label">Etiketler</label>
                        <input type="text" class="form-control" id="tags" name="tags" 
                               value="<%= post.tags ? post.tags.join(', ') : '' %>"
                               placeholder="teknoloji, yazılım, geliştirme (virgülle ayırın)">
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">Güncelle</button>
                        <a href="/posts/<%= post._id %>" class="btn btn-secondary">İptal</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>